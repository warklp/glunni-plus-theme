<style>
  .atlas-isolation-wrapper .rte:after {
    clear: both;
    content: '';
    display: block;
  }

  .atlas-isolation-wrapper .rte > p:first-child {
    margin-top: 0;
  }

  .atlas-isolation-wrapper .rte > p:last-child {
    margin-bottom: 0;
  }

  .atlas-isolation-wrapper .rte table {
    table-layout: fixed;
  }

  @media screen and (min-width: 750px) {
    .atlas-isolation-wrapper .rte table td {
      padding-left: 1.2rem;
      padding-right: 1.2rem;
    }
  }

  .atlas-isolation-wrapper .rte img {
    height: auto;
    max-width: 100%;
    border: var(--atlas-media-border-width) solid rgba(var(--atlas-color-foreground), var(--atlas-media-border-opacity));
    border-radius: var(--atlas-media-radius);
    box-shadow: var(--atlas-media-shadow-horizontal-offset) var(--atlas-media-shadow-vertical-offset)
      var(--atlas-media-shadow-blur-radius) rgba(var(--atlas-color-shadow), var(--atlas-media-shadow-opacity));
    margin-bottom: var(--atlas-media-shadow-vertical-offset);
  }

  .atlas-isolation-wrapper .rte ul,
  .atlas-isolation-wrapper .rte ol {
    list-style-position: inside;
    padding-left: 2rem;
  }

  .atlas-isolation-wrapper .rte li {
    list-style: inherit;
  }

  .atlas-isolation-wrapper .rte li:last-child {
    margin-bottom: 0;
  }

  .atlas-isolation-wrapper .rte a {
    color: rgba(var(--atlas-color-link), var(--atlas-alpha-link));
    text-underline-offset: 0.3rem;
    text-decoration-thickness: 0.1rem;
    transition: text-decoration-thickness var(--atlas-duration-short) ease;
  }

  .atlas-isolation-wrapper .rte a:hover {
    color: rgb(var(--atlas-color-link));
    text-decoration-thickness: 0.2rem;
  }

  .atlas-isolation-wrapper .rte blockquote {
    display: inline-flex;
  }

  .atlas-isolation-wrapper .rte blockquote > * {
    margin: -0.5rem 0 -0.5rem 0;
  }
</style>
