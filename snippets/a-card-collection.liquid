{% comment %}
  Renders a collection card

  Accepts:
  - card_collection: {Object} Collection Liquid object
  - media_aspect_ratio: {String} Size of the product image card. Values are "square" and "portrait". Default is "square" (optional)
  - columns: {Number}
  - extend_height: {Boolean} Card height extends to available container space. Default: false (optional)
  - wrapper_class: {String} Wrapper class for card (optional)

  Usage:
  {% render 'a-card-collection' %}
{% endcomment %}
{% if is_collage %}
  {% assign section_id = 'shopify-section-' | append: section.id %}
  <style>
    .atlas-isolation-wrapper #{{ section_id }} .collage__item--collection .card__content {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      height: 100%;
      display: flex;
      justify-content: center;
      align-items: flex-end;
      padding-bottom: 26px;
    }
    .atlas-isolation-wrapper #{{ section_id }} .collage__item--collection .card__content::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(180deg, rgba({{ overlay_gradient_color.red }}, {{ overlay_gradient_color.green }}, {{ overlay_gradient_color.blue }}, 0) 0%, rgba({{ overlay_gradient_color.red }}, {{ overlay_gradient_color.green }}, {{ overlay_gradient_color.blue }}, 1) 157%);
      z-index: 1;
      {% if border_radius %}
      border-radius: {{ border_radius }}px;
      {% endif %}
    }
    .atlas-isolation-wrapper #{{ section_id }} .collage__item--collection .card__content .card__information{
      z-index: 3;
      padding-bottom: 0;
    }
    .atlas-isolation-wrapper #{{ section_id }} .collage__item--collection .card__content .card__information .image-with-text__text{
      color: {{ text_color }};
      font-size: {{ text_size }}px !important;
      text-align: center;
    }
    .atlas-isolation-wrapper #{{ section_id }} .collage__item--collection .card-wrapper {
      position: relative;
      overflow: hidden;
    }
  </style>
{% endif %}
{%- liquid
  assign ratio = 1
  if card_collection.featured_image and media_aspect_ratio == 'portrait'
    assign ratio = 0.8
  elsif card_collection.featured_image and media_aspect_ratio == 'adapt'
    assign ratio = card_collection.featured_image.aspect_ratio
  endif
  if ratio == 0 or ratio == null
    assign ratio = 1
  endif

  if wrapper_class == null or wrapper_class == 'none'
    assign card_color_scheme = settings.collection_card_color_scheme
    if custom_color_scheme
      assign card_color_scheme = custom_color_scheme
    endif
    assign card_style = settings.collection_card_style
  endif
-%}
<style>
  {% if border_radius %}
    #shopify-section-{{ section.id }} .collage__item--collection .card__media {
      border-radius: {{ border_radius }}px;
    }
  {% endif %}
</style>
<div class="card-wrapper animate-arrow {% if wrapper_class and wrapper_class != 'none' %}{{ wrapper_class }}{% else %}collection-card-wrapper{% endif %}">
  <a
    class="
      card
      card--{{ card_style }}
      {% if card_collection.featured_image %} card--media{% else %} card--text{% endif %}
      {% if card_style == 'card' %} color-{{ card_color_scheme }} gradient{% endif %}
      {% if extend_height %} card--extend-height{% endif %}
      {% if card_collection.featured_image == nil and card_style == 'card' %} ratio{% endif %}
    "
    {% if card_collection == blank %}
      role="link" aria-disabled="true"
    {% else %}
      href="{{ card_collection.url }}"
    {% endif %}
  >
    <div
      class="card__inner {% if card_style == 'standard' %}color-{{ card_color_scheme }} gradient{% endif %}{% if card_collection.featured_image or card_style == 'standard' %} ratio{% endif %}"
      style="--atlas-ratio-percent: {{ 1 | divided_by: ratio | times: 100 }}%;"
    >
      {%- if card_collection.featured_image -%}
        <div class="card__media">
          <div class="media media--transparent media--hover-effect">
            <img
              srcset="
                {%- if card_collection.featured_image.width >= 165 -%}{{ card_collection.featured_image | image_url: width: 165 }} 165w,{%- endif -%}
                {%- if card_collection.featured_image.width >= 330 -%}{{ card_collection.featured_image | image_url: width: 330 }} 330w,{%- endif -%}
                {%- if card_collection.featured_image.width >= 535 -%}{{ card_collection.featured_image | image_url: width: 535 }} 535w,{%- endif -%}
                {%- if card_collection.featured_image.width >= 750 -%}{{ card_collection.featured_image | image_url: width: 750 }} 750w,{%- endif -%}
                {%- if card_collection.featured_image.width >= 1000 -%}{{ card_collection.featured_image | image_url: width: 1000 }} 1000w,{%- endif -%}
                {%- if card_collection.featured_image.width >= 1500 -%}{{ card_collection.featured_image | image_url: width: 1500 }} 1500w,{%- endif -%}
                {%- if card_collection.featured_image.width >= 3000 -%}{{ card_collection.featured_image | image_url: width: 3000 }} 3000w,{%- endif -%}
                {{ card_collection.featured_image | image_url }} {{ card_collection.featured_image.width }}w
              "
              src="{{ card_collection.featured_image | image_url: width: 1500 }}"
              sizes="
                (min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 100 | divided_by: columns }}px,
                (min-width: 750px) {% if columns > 1 %}calc((100vw - 10rem) / 2){% else %}calc(100vw - 10rem){% endif %},
                calc(100vw - 3rem)
              "
              alt=""
              height="{{ card_collection.featured_image.height }}"
              width="{{ card_collection.featured_image.width }}"
              loading="lazy"
              class="motion-reduce"
            >
          </div>
        </div>
      {%- endif -%}
      <div class="card__content">
        <div class="card__information">
          <h3 class="card__heading">
            <div
              class="full-unstyled-link"
            >
              {%- if card_collection.title != blank -%}
                <div class="sw-base-text-color image-with-text__text rte body">
                  <p>
                    {% comment %}
                      {{- card_collection.title | escape -}}
                    {% endcomment %}
                  </p>
                </div>
              {%- else -%}
                <div class="sw-base-text-color image-with-text__text rte body">
                  <p>Your collection's name</p>
                </div>
                {%- if card_collection.description == blank -%}
                  <span class="icon-wrap">{% render 'a-icon-arrow' %}</span>
                {%- endif %}
              {%- endif -%}
            </div>
          </h3>
          {%- if card_collection.description != blank -%}
            <p class="card__caption">
              {{- card_collection.description | strip_html | truncatewords: 12 -}}
              <span class="icon-wrap">&nbsp;{% render 'a-icon-arrow' %}</span>
            </p>
          {%- endif -%}
        </div>
      </div>
    </div>
    {% if card_style == 'card' or card_collection.featured_image %}
      <div class="card__content">
        <div class="card__information">
          <h3 class="card__heading">
            <div
              class="full-unstyled-link"
            >
              {%- if card_collection.title != blank -%}
                <div class="sw-base-text-color image-with-text__text rte body card_collection__title">
                  <p>
                    {{- card_collection.title | escape -}}
                  </p>
                  {% if show_stats %}
                    <div class="card_collection__stats">
                      <div class="card_collection__products_count">{{ card_collection.products | size }} Products</div>
                      <svg
                        class="card_collection__stats__separator"
                        width="6"
                        height="6"
                        viewBox="0 0 6 6"
                        fill="none"
                        xmlns="http://www.w3.org/2000/svg"
                      >
                        <circle cx="3" cy="3" r="3" fill="var(--atlas-color-fill)"/>
                      </svg>
                      <div class="card_collection__lowest-price">
                        {% assign lowest_price = card_collection.products | sort: 'price' | first %}
                        Starts from {{ lowest_price.price | money }}
                      </div>
                    </div>
                  {% endif %}
                </div>
              {%- else -%}
                <div class="sw-base-text-color image-with-text__text rte body">
                  <p>Your collection's name</p>
                </div>
              {%- endif -%}
              {%- if card_collection.featured_image or card_collection.description == blank -%}
                {% if is_collage == false %}
                  <span class="icon-wrap">{% render 'a-icon-arrow' %}</span>
                {% endif %}
              {%- endif %}
            </div>
          </h3>
          {%- if card_collection.featured_image == null and card_collection.description != blank -%}
            <p class="card__caption">
              {{- card_collection.description | strip_html | truncatewords: 12 -}}
              <span class="icon-wrap">&nbsp;{% render 'a-icon-arrow' %}</span>
            </p>
          {%- endif -%}
        </div>
      </div>
    {% endif %}
  </a>
</div>
