{{ 'a-template-collection.css' | asset_url | stylesheet_tag }}
{{ 'a-component-card.css' | asset_url | stylesheet_tag }}
{{ 'a-component-article-card.css' | asset_url | stylesheet_tag }}
{% render 'a-main-product-css-r2' %}
  <link rel="stylesheet" href="{{ 'a-quick-add-r2.css' | asset_url }}" media="print" onload="this.media='all'">
  <script src="{{ 'a-quick-add-r2.js' | asset_url }}" defer="defer"></script>

<link rel="stylesheet" href="{{ 'a-component-search.css' | asset_url }}" media="print" onload="this.media='all'">

<noscript>{{ 'a-component-search.css' | asset_url | stylesheet_tag }}</noscript>

{%- if section.settings.enable_filtering or section.settings.enable_sorting -%}
  {{ 'a-component-facets.css' | asset_url | stylesheet_tag }}
  <script src="{{ 'a-facets.js' | asset_url }}" defer="defer"></script>
{%- endif -%}

<script src="{{ 'a-main-search.js' | asset_url }}" defer="defer"></script>

<style>
  .template-search__header {
    margin-bottom: 3rem;
  }

  .template-search__search {
    margin: 0 auto 3.5rem;
    max-width: 74.1rem;
  }

  .template-search__search .search {
    margin-top: 3rem;
  }

  .template-search--empty {
    padding-bottom: 18rem;
  }
  .template-search__results .card__heading {
    font-size: 1.6rem;
  }

  @media screen and (min-width: 750px) {
    .template-search__header {
      margin-bottom: 5rem;
    }
  }

  .search__button .icon {
    height: 1.8rem;
  }
</style>

{%- liquid
  assign sort_by = search.sort_by | default: search.default_sort_by
  assign terms = search.terms | escape
  assign search_url = '?q=' | append: terms | append: '&options%5Bprefix%5D=last&sort_by=' | append: sort_by
-%}

{%- style -%}
  {%- if request.visual_preview_mode -%}
    {% comment %} don't want fade-in animation in preview mode because it messes up preview {% endcomment %}
    .animation_section,
    .animation_section * {
      opacity: 1 !important;
      transform: none !important;
      transition: none !important;
      will-change: auto !important;
    }
  {%- endif -%}
  #shopify-section-{{ section.id }} .product-grid {
    gap: {{ section.settings.grid_gap_vertical_desktop }}px {{ section.settings.grid_gap_horizontal_desktop }}px ;
    grid-template-columns: repeat({{ section.settings.columns_desktop }}, 1fr);
    display: grid;
    list-style: none;
    margin: 0;
    padding: 0;
    margin-top: {{ section.settings.space_desktop_grid_and_filters }}px;
  }
  #shopify-section-{{ section.id }} .product-grid li {
    width: 100%;
    max-width: 100%;
  }
  @media screen and (max-width: 768px) {
    #shopify-section-{{ section.id }} .product-grid {
      gap:  {{ section.settings.grid_gap_vertical_mobile }}px {{ section.settings.grid_gap_horizontal_mobile }}px;
      grid-template-columns: repeat({{ section.settings.columns_mobile }}, 1fr);
      margin-top: {{ section.settings.space_mobile_grid_and_filters }}px;
    }
  }
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  .section-{{section.id}} span.price-item.price-item--regular {
    font-size: {{ section.settings.regular_price_font_size }}px;
  }



  @media screen and (max-width: 990px) {
    .section-{{section.id}} .price__compare-price {
      line-height: 20px;
    }
  }

  .facets__heading,.facets__summary,.facet-filters__sort,.facet-filters__label,.product-count * {
    font-size: var(--atlas-facets-desktop-header-font-size) !important;
    line-height: calc(var(--atlas-facets-desktop-header-font-size) + 8px) !important;
    font-weight: 400 !important;
  }
  .facets__heading,.facet-filters__sort,.facet-filters__label,.product-count * {
    color: var(--atlas-facets-color-primary) !important;
  }
  .facets__summary {
    color: var(--atlas-facets-color-secondary) !important;
  }
{%- endstyle -%}

<div class="template-search{% unless search.performed and search.results_count > 0 %} template-search--empty{% endunless %} section-{{ section.id }}-padding">
  <div class="template-search__header page-width">
    <h1 class="h2 center">
      {%- if search.performed -%}
        Search results
      {%- else -%}
        Search
      {%- endif -%}
    </h1>
    <div class="template-search__search">
      {%- if settings.predictive_search_enabled -%}
        <predictive-search data-loading-text="Loading...">
      {%- endif -%}
          <main-search>
            <form action="{{ routes.search_url }}" method="get" role="search" class="search">
              <div class="field">
                <input
                  class="search__input field__input"
                  id="Search-In-Template"
                  type="search"
                  name="q"
                  value="{{ search.terms | escape }}"
                  placeholder="Search"
                  {%- if settings.predictive_search_enabled -%}
                    role="combobox"
                    aria-expanded="false"
                    aria-owns="predictive-search-results"
                    aria-controls="predictive-search-results"
                    aria-haspopup="listbox"
                    aria-autocomplete="list"
                    autocorrect="off"
                    autocomplete="off"
                    autocapitalize="off"
                    spellcheck="false"
                  {%- endif -%}
                >
                <label class="field__label" for="Search-In-Template">Search</label>
                <input name="options[prefix]" type="hidden" value="last">

                {%- if settings.predictive_search_enabled -%}
                  <div class="predictive-search predictive-search--search-template" tabindex="-1" data-predictive-search>
                    <div class="predictive-search__loading-state">
                      <svg aria-hidden="true" focusable="false" class="spinner" viewBox="0 0 66 66" xmlns="http://www.w3.org/2000/svg">
                        <circle class="path" fill="none" stroke-width="6" cx="33" cy="33" r="30"></circle>
                      </svg>
                    </div>
                  </div>

                  <span class="predictive-search-status visually-hidden" role="status" aria-hidden="true"></span>
                {%- endif -%}

                <button type="reset" class="reset__button field__button{% if search.terms == blank %} hidden{% endif %}" aria-label="Clear search term">
                  <svg class="icon icon-close" aria-hidden="true" focusable="false">
                    <use xlink:href="#icon-reset">
                  </svg>
                </button>
                <button type="submit" class="search__button field__button" aria-label="Search">
                  <svg class="icon icon-search" aria-hidden="true" focusable="false">
                    <use xlink:href="#icon-search">
                  </svg>
                </button>
              </div>
            </form>
          </main-search>
      {%- if settings.predictive_search_enabled -%}
        </predictive-search>
      {%- endif -%}

    </div>
    {%- if search.performed -%}
      {%- unless section.settings.enable_filtering or section.settings.enable_sorting -%}
        {%- if search.results_count > 0 -%}
          <p role="status">{% if search.results_count == 1 %}{{ search.results_count }} result found for "{{ search.terms }}"{% else %}{{ search.results_count }} results found for "{{ search.terms }}"{% endif %}</p>
        {%- endif -%}
      {%- endunless -%}
      {%- if search.results_count == 0 and search.filters == empty -%}
        <p role="status">No results found for "{{ search.terms }}". Check the spelling or use a different word or phrase.</p>
      {%- endif -%}
    {%- endif -%}
  </div>
  {%- if search.performed -%}
    {% comment %} {%- if section.settings.enable_sorting and section.settings.filter_type == 'vertical' and search.filters != empty -%}
      <facet-filters-form class="facets facets-vertical-sort page-width small-hide no-js-hidden">
        <form class="facets-vertical-form" id="FacetSortForm">
          <div class="facet-filters sorting caption">
            <div class="facet-filters__field">
              <h2 class="facet-filters__label caption-large text-body">
                <label for="SortBy">Sort by:</label>
              </h2>
              <div class="select">
                {%- assign sort_by = search.sort_by | default: search.default_sort_by -%}
                <select name="sort_by" class="facet-filters__sort select__select caption-large" id="SortBy" aria-describedby="a11y-refresh-page-message">
                  {%- for option in search.sort_options -%}
                    <option value="{{ option.value | escape }}"{% if option.value == sort_by %} selected="selected"{% endif %}>{{ option.name | escape }}</option>
                  {%- endfor -%}
                </select>
                {% render 'a-icon-caret' %}
              </div>
            </div>
            <noscript>
              <button type="submit" class="facets__button-no-js button button--secondary">Sort</button>
            </noscript>
          </div>

          <div class="product-count-vertical light" role="status">
            <h2 class="product-count__text text-body">
              <span id="ProductCountDesktop">
                {%- if search.results_count -%}
                  {% if search.results_count == 1 %}{{ search.results_count }} result found for "{{ search.terms }}"{% else %}{{ search.results_count }} results found for "{{ search.terms }}"{% endif %}
                {%- elsif search.products_count == search.all_products_count -%}
                  {% if search.products_count == 1 %}{{ search.products_count }} product{% else %}{{ search.products_count }} products{% endif %}
                {%- else -%}
                  {{ search.products_count }} of {{ search.all_products_count }} {% if search.all_products_count == 1 %}product{% else %}products{% endif %}
                {%- endif -%}
              </span>
            </h2>
            <div class="loading-overlay__spinner">
              <svg aria-hidden="true" focusable="false" class="spinner" viewBox="0 0 66 66" xmlns="http://www.w3.org/2000/svg">
                <circle class="path" fill="none" stroke-width="6" cx="33" cy="33" r="30"></circle>
              </svg>
            </div>
          </div>
        </form>
      </facet-filters-form>
    {%- endif -%} {% endcomment %}
    <div>
      {%- if search.filters != empty -%}
        {%- if section.settings.enable_filtering or section.settings.enable_sorting -%}
          <aside aria-labelledby="verticalTitle" class="facets-wrapper{% unless section.settings.enable_filtering %} facets-wrapper--no-filters{% endunless %}{% if section.settings.filter_type != 'vertical' %} page-width{% endif %}" id="main-search-filters" data-id="{{ section.id }}">
            {% render 'a-facets', results: search, enable_filtering: section.settings.enable_filtering, enable_sorting: section.settings.enable_sorting, filter_type: 'horizontal' %}
          </aside>
        {%- endif -%}
      {%- endif -%}
      <div class="product-grid-container" id="ProductGridContainer">
        {%- if search.results.size == 0 and search.filters != empty -%}
          <div class="template-search__results collection collection--empty page-width" id="product-grid" data-id="{{ section.id }}">
            <div class="loading-overlay gradient"></div>
            <div class="title-wrapper center">
              <h2 class="title title--primary">
                No products found<br>
                Use fewer filters or <a class="underlined-link link" href="{{ search_url }}">remove all</a>
              </h2>
            </div>
          </div>
        {%- else -%}
          {% paginate search.results by section.settings.searches_per_page %}
            <div class="template-search__results collection page-width" id="product-grid" data-id="{{ section.id }}">
              <div class="loading-overlay gradient"></div>
              <ul class="product-grid" role="list">
                {%- for item in search.results -%}
                  {% if section.settings.results_include_products==false and item.object_type == 'product' %}
                    {% continue %}
                  {% endif %}
                  {% if section.settings.results_include_articles==false and item.object_type == 'article' %}
                    {% continue %}
                  {% endif %}
                  {% if section.settings.results_include_pages==false and item.object_type == 'page' %}
                    {% continue %}
                  {% endif %}
                  {% assign lazy_load = false %}
                  {%- if forloop.index > 2 -%}
                    {%- assign lazy_load = true -%}
                  {%- endif -%}

                  <li class="grid__item">
                    {%- case item.object_type -%}
                      {%- when 'product' -%}
                        {%- capture product_settings -%}{%- if section.settings.product_show_vendor -%}vendor,{%- endif -%}title,price{%- endcapture -%}
                        {% render 'a-card-product-r4',
                          card_product: item,
                          show_secondary_image: true,
                          show_rating: true,
                          lazy_load: lazy_load
                        %}
                      {%- when 'article' -%}
                        {% render 'a-article-card',
                          article: item,
                          show_image: true,
                          show_date: true,
                          show_author: true,
                          show_badge: true,
                          media_aspect_ratio: 1,
                          lazy_load: lazy_load
                        %}
                      {%- when 'page' -%}
                        <div class="article-card-wrapper card-wrapper underline-links-hover">
                          <div class="card card--card card--text ratio color-{{ settings.blog_card_color_scheme }}" style="--atlas-ratio-percent: 100%;">
                            <div class="card__content">
                              <div class="card__information">
                                <h3 class="card__heading">
                                  <a href="{{ item.url }}" class="full-unstyled-link">
                                    {{ item.title | truncate: 50 | escape }}
                                  </a>
                                </h3>
                              </div>
                              <div class="card__badge {{ settings.badge_position }}">
                                <span class="badge color-accent-1">Page</span>
                              </div>
                            </div>
                          </div>
                        </div>
                    {%- endcase -%}
                  </li>
                {%- endfor -%}
              </ul>
              {%- if paginate.pages > 1 -%}
                {% render 'a-pagination', paginate: paginate %}
              {%- endif -%}
            </div>
          {% endpaginate %}
        {%- endif -%}
      </div>
    </div>
  {%- endif -%}
</div>

{% schema %}
{
  "name": "Search results",
  "class": "section animation_section",
  "settings": [
    {
      "type": "range",
      "id": "searches_per_page",
      "min": 8,
      "max": 32,
      "step": 4,
      "default": 24,
      "label": "Number of search results per page"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 1,
      "max": 5,
      "step": 1,
      "default": 4,
      "label": "Number of columns on desktop"
    },
    {
      "type": "header",
      "content": "Filtering and sorting"
    },
    {
      "type": "checkbox",
      "id": "enable_filtering",
      "default": true,
      "label": "Enable filtering",
      "info": "Customize filters with the Search & Discovery app. [Learn more](https://help.shopify.com/manual/online-store/search-and-discovery/filters)"
    },
    {
      "type": "checkbox",
      "id": "enable_sorting",
      "default": true,
      "label": "Enable sorting"
    },
    {
      "type": "checkbox",
      "id": "results_include_articles",
      "default": false,
      "label": "Include articles in search results"
    },
    {
      "type": "checkbox",
      "id": "results_include_pages",
      "default": false,
      "label": "Include pages in search results"
    },
    {
      "type": "checkbox",
      "id": "results_include_products",
      "default": true,
      "label": "Include products in search results"
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "columns_mobile",
      "default": "2",
      "label": "Number of columns on mobile",
      "options": [
        {
          "value": "1",
          "label": "1 column"
        },
        {
          "value": "2",
          "label": "2 columns"
        }
      ]
    },
    {
      "type": "range",
      "id": "grid_gap_horizontal_desktop",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Desktop grid gap horizontal",
      "default": 20
    },
    {
      "type": "range",
      "id": "grid_gap_horizontal_mobile",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Mobile grid gap horizontal",
      "default": 10
    },
    {
      "type": "range",
      "id": "grid_gap_vertical_desktop",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Desktop grid gap vertical",
      "default": 20
    },
    {
      "type": "range",
      "id": "grid_gap_vertical_mobile",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Mobile grid gap vertical",
      "default": 10
    },
    {
      "type": "range",
      "id": "space_desktop_grid_and_filters",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Desktop space between grid and filters",
      "default": 20
    },
    {
      "type": "range",
      "id": "space_mobile_grid_and_filters",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Mobile space between grid and filters",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 36
    }
  ]
}
{% endschema %}
