<!-- start _reasons-to-buy-stats.liquid -->
<style>
  {% if request.visual_preview_mode %}
    {% assign section_id_selector = '' %}
    {% assign section_id_variable = 'id' %}
  {% else %}
    {% assign section_id_selector = '#shopify-section-' | append: section.id %}
    {% assign section_id_variable = section.id %}
  {% endif %}
    {{ section_id_selector }} .reasons-to-buy__stats-content {
      display: flex;
      flex-direction: column;
      justify-content: {{ block.settings.content_alignment }};
      height: auto;
      flex: unset;
      grid-row: 1 / 4;
      grid-column: 3 / 4;
    }
    {{ section_id_selector }} .reasons-to-buy__stats-content-item-container {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: {{ block.settings.cards_vertical_spacing }}px 0;
      border-bottom: 1px solid  rgba({{ block.settings.border_color.red }}, {{ block.settings.border_color.green }}, {{ block.settings.border_color.blue }}, 0.05);
    }
    {{ section_id_selector }} .reasons-to-buy__stats-content-item-percentage {
      font-size: {{ block.settings.percentage_number_font_size }}px;
      font-weight: 900;
      line-height: 100%;
      
      {% if block.settings.percentage_number_has_gradient %}
      background-clip: text;
      color: transparent;
      background-image: {{ block.settings.percentage_number_bg }};
      {% else %}
      color: {{ block.settings.percentage_number_color }};
      {% endif %}
      display: flex;
    }
    {%- comment -%}
      Gated: hide the count-up number so a row shows only its heading/body. Off by default,
      so every other template still renders the number. Used by price-ladder style rows
      whose value (a price) can't live in the 0–100 percentage field.
    {%- endcomment -%}
    {%- if block.settings.hide_stat_number -%}
      {{ section_id_selector }} .reasons-to-buy__stats-content-item-percentage { display: none; }
    {%- endif -%}
    {%- if block.settings.chip_shape != blank and block.settings.chip_shape != 'none' -%}
      {%- comment -%}
        number_chip_color is a Color drop that stringifies to rgba(...), not hex, so a
        raw "#{{ color }}" produced an invalid SVG fill and the chip rendered black.
        Build an rgb() fill from the channels instead (commas encoded as %2C so the
        data-URI decodes them back to commas for the SVG parser).
      {%- endcomment -%}
      {%- assign chip_fill = 'rgb(' | append: block.settings.number_chip_color.red | append: '%2C' | append: block.settings.number_chip_color.green | append: '%2C' | append: block.settings.number_chip_color.blue | append: ')' -%}
      {%- if block.settings.chip_shape == 'circle' -%}
        {%- assign chip_svg = "%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 64 64'%3E%3Ccircle cx='32' cy='32' r='32' fill='" | append: chip_fill | append: "'/%3E%3C/svg%3E" -%}
        {%- assign chip_pad_bottom = 0 -%}
      {%- elsif block.settings.chip_shape == 'ring' -%}
        {%- assign chip_svg = "%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 64 64'%3E%3Ccircle cx='32' cy='32' r='30' fill='none' stroke='" | append: chip_fill | append: "' stroke-width='3'/%3E%3C/svg%3E" -%}
        {%- assign chip_pad_bottom = 0 -%}
      {%- else -%}
        {%- assign chip_svg = "%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 72 64'%3E%3Cpath fill='" | append: chip_fill | append: "' d='M20.5 60A32 32 0 1 1 51.5 60Q36 44 20.5 60Z'/%3E%3C/svg%3E" -%}
        {%- assign chip_pad_bottom = 6 -%}
      {%- endif -%}
      {{ section_id_selector }} .reasons-to-buy__stats-content-item-percentage {
        position: relative;
        min-width: 92px;
        height: 84px;
        align-items: center;
        justify-content: center;
        padding-bottom: {{ chip_pad_bottom }}px;
      }
      {{ section_id_selector }} .reasons-to-buy__stats-content-item-percentage::before {
        content: "";
        position: absolute;
        inset: 0;
        z-index: 0;
        background-image: url("data:image/svg+xml,{{ chip_svg }}");
        background-repeat: no-repeat;
        background-position: center;
        background-size: contain;
      }
      {{ section_id_selector }} .reasons-to-buy__stats-content-item-percentage strong {
        position: relative;
        z-index: 1;
      }
    {%- endif -%}
    {{ section_id_selector }} .reasons-to-buy__stats-content-item {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }
    @media screen and (max-width: 768px) {
      {{ section_id_selector }} .reasons-to-buy__stats-content-item {
        gap: 6px;
      }
    }
    {{ section_id_selector }} .reasons-to-buy__stats-content-item-heading {
      font-size: {{ block.settings.block_heading_size }}px;
      font-weight: 500;
      color: {{ block.settings.block_heading_color }};
      line-height: 100%;
    }
    {{ section_id_selector }} .reasons-to-buy__stats-content-item-heading strong {
      color: {{ block.settings.bolded_block_heading_color }};
    }
    {{ section_id_selector }} .reasons-to-buy__stats-content-item-heading p {
      margin: 0;
    }
    {{ section_id_selector }} .reasons-to-buy__stats-content-item-text {
      font-size: {{ block.settings.block_body_size }}px;
      font-weight: 300;
      line-height: 100%;
      color: {{ block.settings.block_body_color }};
    }
    {{ section_id_selector }} .reasons-to-buy__stats-content-item-text strong {
      color: {{ block.settings.bolded_block_body_color }};
    }
    {{ section_id_selector }} .reasons-to-buy__stats-content-item-text p {
      margin: 0;
    }
    @media screen and (max-width: 1200px) and (min-width: 768px) {
      {{ section_id_selector }} .reasons-to-buy__stats-content {
        grid-row: 3 / 4;
        grid-column: 1 / 2;
        margin-top: 16px;
      }
    }
    @media screen and (max-width: 768px) {
      {{ section_id_selector }} .reasons-to-buy__stats-content {
        grid-row: 4 / 5;
        grid-column: 1 / 2;
        margin-top: 16px;
      }
    }
    @media screen and (max-width: 1200px) {
    {{ section_id_selector }} .reasons-to-buy__stats-content-item-container {
      padding: {{ block.settings.cards_vertical_spacing_mobile }}px 0 ;
    }
    {{ section_id_selector }} .reasons-to-buy__stats-content-item-percentage {
      min-width: unset !important;
      font-size: {{ block.settings.percentage_number_font_size_mobile }}px !important;
    }
    {{ section_id_selector }} .reasons-to-buy__stats-content-item-heading{
      font-size: {{ block.settings.block_heading_size_mobile }}px !important;
    }
    {{ section_id_selector }} .reasons-to-buy__stats-content-item-text {
      font-size: {{ block.settings.block_body_size_mobile }}px !important;
    }
  }

  {%- comment -%}
    Keep the chip a fixed width on mobile (the rule above unsets min-width) so every chip
    is the same size regardless of how many characters the number has. Placed after the
    general mobile rule so its !important min-width wins.
  {%- endcomment -%}
  {%- if block.settings.chip_shape != blank and block.settings.chip_shape != 'none' -%}
    @media screen and (max-width: 1200px) {
      {{ section_id_selector }} .reasons-to-buy__stats-content-item-percentage {
        min-width: {{ block.settings.chip_size_mobile }}px !important;
        height: {{ block.settings.chip_size_mobile | times: 0.92 | round }}px !important;
        padding-bottom: {% if block.settings.chip_shape == 'blob' %}4px{% else %}0{% endif %} !important;
      }
    }
  {%- endif -%}

    {{ section_id_selector }} .reasons-to-buy__stats-content-item-container:last-of-type{
        border-bottom: none;
    }
  </style>

<div class="reasons-to-buy__stats-content" {{ block.shopify_attributes }}>
  {% content_for 'blocks' %}
</div>
<script>
    function initStatsAnimation{{ section_id_variable | replace: '-', '' }} () {
      const statsBlocksPercentageValues = this.querySelectorAll("{{ section_id_selector }} .reasons-to-buy__stats-content-item-container");
      const statsData = []
      if(statsBlocksPercentageValues.length === 0) return;
      statsBlocksPercentageValues.forEach(block => {
        statsData.push({
          value: parseInt(block.getAttribute("percentage-value")),
          hidePercent: block.getAttribute("data-hide-percent") === "true",
          suffix: block.getAttribute("data-suffix") || ""
        })
      });
      const percentageElements = document.querySelectorAll('{{ section_id_selector }} .reasons-to-buy__stats-content-item-percentage strong');
      const section = document.querySelector('{{ section_id_selector }} .reasons-to-buy__container'); 
      let animationTriggered = false; 
      
      function animatePercentage(percentElement, targetPercentage, hidePercent, suffix) {
        if (!percentElement) return;

        const duration = 1800;
        const startTime = performance.now();
        const sign = hidePercent ? '' : '%';
        const suf = suffix || '';

        function updatePercentage(currentTime) {
          const elapsed = currentTime - startTime;
          const t = Math.min(elapsed / duration, 1);
          const easeOut = 1 - Math.pow(1 - t, 3);
          const currentProgress = easeOut * targetPercentage;

          percentElement.textContent = `${Math.round(currentProgress)}${sign}${suf}`;
  
          if (elapsed < duration) {
            requestAnimationFrame(updatePercentage);
          }
        }
  
        requestAnimationFrame(updatePercentage);
      }
  
      function animateAllPercentages() {
        if (animationTriggered) return;
        animationTriggered = true; 
  
        percentageElements.forEach((element, index) => {
          if (statsData[index]) {
            const delay = index * 150;
            setTimeout(() => {
              animatePercentage(element, statsData[index].value, statsData[index].hidePercent, statsData[index].suffix);
            }, delay);
          }
        });
      }
  
      if (section) {
        const observer = new IntersectionObserver(entries => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              animateAllPercentages();
              observer.disconnect(); 
            }
          });
        }, { threshold: 0.3 });
  
        observer.observe(section);
      }
    }
  
    document.addEventListener('DOMContentLoaded', initStatsAnimation{{ section_id_variable | replace: '-', '' }});
  
    if (Shopify.designMode) {
      const sectionId = "{{ section_id_variable }}";
    
      document.addEventListener('shopify:section:unload', (event) => {
        if (event.detail.sectionId === sectionId) {
          initStatsAnimation{{ section_id_variable | replace: '-', '' }}();
        }
      });
    
      document.addEventListener('shopify:section:load', (event) => {
        if (event.detail.sectionId === sectionId) {
          initStatsAnimation{{ section_id_variable | replace: '-', '' }}();
        }
      });
    }
</script>
<!-- end _reasons-to-buy-stats.liquid -->
{% schema %}
{
  "name": "Reasons to buy stats",
  "tag": null,
  "blocks": [
    {
      "type": "_a-reasons-to-buy-stat-r2"
    }
  ],
  "settings": [
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "content_alignment",
      "label": "Cards content alignment",
      "options": [
        { "value": "flex-start", "label": "Top" },
        { "value": "center", "label": "Center" },
        { "value": "flex-end", "label": "Bottom" }
      ],
      "default": "flex-start"
    },
    {
      "type": "range",
      "id": "cards_vertical_spacing",
      "label": "Desktop cards vertical spacing",
      "min": 4,
      "max": 64,
      "step": 1,
      "unit": "px",
      "default": 38
    },
    {
        "type": "range",
        "id": "cards_vertical_spacing_mobile",
        "label": "Mobile cards vertical spacing",
        "min": 4,
        "max": 32,
        "step": 1,
        "unit": "px",
        "default": 14
    },
    {
      "type": "header",
      "content": "Typography"
    },
    {
      "type": "checkbox",
      "id": "hide_stat_number",
      "label": "Hide the number",
      "default": false,
      "info": "Hides the count-up number so each row shows only its heading and body. Use for price rows where the value isn't a 0–100 percentage."
    },
    {
      "type": "range",
      "id": "percentage_number_font_size",
      "label": "Desktop percentage number font size",
      "min": 20,
      "max": 70,
      "step": 1,
      "unit": "px",
      "default": 50
    },
    {
      "type": "range",
      "id": "percentage_number_font_size_mobile",
      "label": "Mobile percentage number font size",
      "min": 14,
      "max": 48,
      "step": 1,
      "unit": "px",
      "default": 28
    },
    {
      "type": "color",
      "id": "percentage_number_color",
      "label": "Percentage number color",
      "default": "#000000"
    },
    {
      "type": "checkbox",
      "id": "percentage_number_has_gradient",
      "label": "Use gradient for percentage number",
      "default": true,
      "info": "If enabled, the percentage number will use a gradient instead of a solid color"
    },
    {
      "type": "color_background",
      "id": "percentage_number_bg",
      "label": "Percentage number gradient background",
      "default": "linear-gradient(180deg, #EA3885 0%, #5F4DBC 100%)"
    },
    {
      "type": "header",
      "content": "Number chip"
    },
    {
      "type": "select",
      "id": "chip_shape",
      "label": "Chip behind number",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "blob", "label": "Notched circle" },
        { "value": "circle", "label": "Complete circle" },
        { "value": "ring", "label": "Circle outline" }
      ],
      "default": "none",
      "info": "Places each percentage number on a chip shape."
    },
    {
      "type": "color",
      "id": "number_chip_color",
      "label": "Chip color",
      "default": "#F6C445",
      "visible_if": "{{ block.settings.chip_shape != 'none' }}"
    },
    {
      "type": "range",
      "id": "chip_size_mobile",
      "label": "Mobile chip width",
      "min": 36,
      "max": 96,
      "step": 2,
      "unit": "px",
      "default": 60,
      "visible_if": "{{ block.settings.chip_shape != 'none' }}"
    },
    {
      "type": "range",
      "id": "block_heading_size",
      "label": "Desktop heading font size",
      "min": 12,
      "max": 32,
      "step": 1,
      "unit": "px",
      "default": 24
    },
    {
      "type": "range",
      "id": "block_heading_size_mobile",
      "label": "Mobile heading font size",
      "min": 12,
      "max": 32,
      "step": 1,
      "unit": "px",
      "default": 14
    },
    {
      "type": "color",
      "id": "block_heading_color",
      "label": "Heading color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "bolded_block_heading_color",
      "label": "Bolded heading color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "block_body_size",
      "label": "Desktop body font size",
      "min": 8,
      "max": 20,
      "step": 1,
      "unit": "px",
      "default": 12
    },
    {
      "type": "range",
      "id": "block_body_size_mobile",
      "label": "Mobile body font size",
      "min": 8,
      "max": 20,
      "step": 1,
      "unit": "px",
      "default": 14
    },
    {
      "type": "color",
      "id": "block_body_color",
      "label": "Body color",
      "default": "#666666"
    },
    {
      "type": "color",
      "id": "bolded_block_body_color",
      "label": "Bolded body color",
      "default": "#666666"
    },
    {
      "type": "header",
      "content": "Appearance"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "Cards separator color",
      "default": "#000000"
    }
  ],
  "presets": [
    {
      "name": "Reasons to buy stats",
      "settings": {
        "content_alignment": "center",
        "cards_vertical_spacing": 25,
        "cards_vertical_spacing_mobile": 14,
        "percentage_number_font_size": 50,
        "percentage_number_font_size_mobile": 28,
        "block_heading_size": 24,
        "block_heading_size_mobile": 14,
        "block_body_size": 12,
        "block_body_size_mobile": 14,
        "percentage_number_bg": "linear-gradient(180deg, rgba(234, 56, 133, 1), rgba(95, 77, 188, 1) 100%)",
        "block_heading_color": "#000000",
        "bolded_block_heading_color": "#000000",
        "block_body_color": "#666666",
        "bolded_block_body_color": "#666666",
        "border_color": "#000000"
      },
      "blocks": [
        {
          "type": "_a-reasons-to-buy-stat-r2",
          "settings": {
            "row_heading": "<p>Customers saw <strong>visible improvement</strong></p>",
            "row_text": "<p>Within the first 2 weeks of use</p>",
            "percentage": 92
          }
        },
        {
          "type": "_a-reasons-to-buy-stat-r2",
          "settings": {
            "row_heading": "<p>Reported <strong>better skin texture</strong></p>",
            "row_text": "<p>Smoother and more even tone</p>",
            "percentage": 87
          }
        },
        {
          "type": "_a-reasons-to-buy-stat-r2",
          "settings": {
            "row_heading": "<p>Felt <strong>more confident</strong></p>",
            "row_text": "<p>After 4 weeks of consistent use</p>",
            "percentage": 94
          }
        }
      ]
    }
  ]
}
{% endschema %}
